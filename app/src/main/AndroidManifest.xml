<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dy.qxjhutil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Test1Activity" />
        <activity android:name=".activity.Edit1Activity" />
        <activity android:name=".activity.WxInfoActivity" />
        <activity android:name=".activity.WxListActivity" />
        <activity android:name=".activity.MapDetailsActivity" />
        <activity android:name=".activity.MapListActivity" />
        <activity android:name=".activity.WxDefaultActivity" />
    </application>

</manifest>